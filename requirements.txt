# Runtime deps for the pip fallback path (uv sync is primary).
# Mirrors [project].dependencies in pyproject.toml.
mcp[cli]>=1.2
neo4j>=5.0
# sqlite3 is part of the Python standard library — no install needed for the SQLite track.
